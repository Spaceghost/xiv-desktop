<Project Sdk="Microsoft.NET.Sdk">
  <!-- Host-side check: scans the real Linux application and icon directories (no Wine mapping) and prints what the plugin would list. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>XivDesktop.Scan</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\XivDesktop.Core\XivDesktop.Core.csproj" />
  </ItemGroup>
</Project>
